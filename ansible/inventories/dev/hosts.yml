---
# Dev inventory.
all:
  children:
    app:
      hosts:
        app1:
          ansible_host: APP_1_PUBLIC_IP_PLACEHOLDER
          application_nat_instance: true
        app2:
          ansible_host: APP_2_PUBLIC_IP_PLACEHOLDER
    database:
      hosts:
        db1:
          ansible_host: DB_PRIVATE_IP_PLACEHOLDER
    loadbalancer:
      hosts: {}
    backup:
      hosts:
        db1:
          ansible_host: DB_PRIVATE_IP_PLACEHOLDER
