---
- name: Prepare
  hosts: all
  become: true
  tasks:
    - name: Ensure cron is installed (required by the backup role)
      ansible.builtin.apt:
        name: cron
        state: present
        update_cache: true
